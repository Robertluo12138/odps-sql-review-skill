# 报告输出模板（output_template）

> 这是 odps-sql-review skill 的 **唯一权威输出格式**。
> AI Agent 在生成评审报告时必须严格按这个 8 个章节的结构输出，
> 章节标题、章节编号、字段名都要保持一致，便于评审同事快速扫描。
>
> 即使没有发现任何风险，也必须输出全部章节，缺项写
> “暂无 / 暂未发现 ... ，仍需结合 LogView / 数据量复核”，
> 不要直接输出 “looks good”。

---

```markdown
# SQL 风险审查报告

## 0. 总体结论
- 总风险等级：高 / 中 / 低
- 是否建议直接上线：是 / 否
- 主要阻塞点：
  - <列出 high + blocking 的问题，没有则写 “暂无明确阻塞点”>
- 主要性能瓶颈：
  - <列出关键性能问题，没有则写 “暂未发现典型性能瓶颈，仍建议结合 LogView / 数据量复核”>
- 需要确认项：
  - <列出影响判断的未知信息（表粒度、唯一键、表大小、LogView、业务定义等）>

## 1. 高风险问题
（每条包含：风险点 / 证据 SQL 片段 / 为什么危险 / 建议修改方式 / 是否阻塞上线）

#### <高风险标题>
- 风险点：<简述>
- 证据 SQL 片段：
  ```sql
  <相关 SQL 片段>
  ```
- 为什么危险：<原因>
- 建议修改方式：<可操作的改写建议>
- 是否阻塞上线：是 / 否

## 2. 中风险问题
（每条包含：风险点 / 证据 SQL 片段 / 影响 / 建议修改方式）

#### <中风险标题>
- 风险点：<简述>
- 证据 SQL 片段：
  ```sql
  <相关 SQL 片段>
  ```
- 影响：<对资源 / 口径 / 报表的影响>
- 建议修改方式：<可操作的改写建议>

## 3. 低风险 / 可读性问题
- SELECT *
- 单字母别名 / 命名不清
- 重复 CASE WHEN
- 缺少注释
- 过长 CTE

（按需列出实际命中的低风险项）

## 4. 性能优化建议
分组输出（缺项写 “暂无明显问题，仍需结合 LogView / 数据量确认”）：

### 分区裁剪
- ...

### Join 优化
- ...

### Group By / Count Distinct 优化
- ...

### 年活 / 月活 / 长周期去重优化
- ...

### 动态分区优化
- ...

### Reducer / Joiner / 并行度建议
- ...
- 没有 LogView 时，必须写：需要结合 LogView / 数据量确认

### 中间表 / 滚动表建议
- ...

> 不要盲目推荐固定的 ODPS SET 数值。
> 缺数据量 / LogView / 资源配额时，必须写
> “需要结合 LogView / 数据量确认”。

## 5. 指标口径解释
按指标列出（每个指标都包含）：

#### 指标：`<alias 或 表达式>`
- 表达式：`<原始表达式>`
- 聚合类型：SUM / COUNT / COUNT_DISTINCT / AVG / MIN / MAX / RATIO / CASE_WHEN / APPROX_DISTINCT
- 分子：<分子，或 “需要确认”>
- 分母：<分母，或 “非比率指标，无分母”>
- 过滤条件：<CASE WHEN 内嵌过滤 + WHERE 中作用于该指标的条件>
- GROUP BY 粒度：<外层 GROUP BY 列>
- 是否存在重复计算风险：是 / 否 / 需要确认
- 是否需要业务确认：是 / 否（默认 “是”）
- 备注：<可选，例如长周期去重指标的中间表建议>

## 6. 建议改写 SQL
- **默认为零 profile 模式**：表粒度 / 唯一键 / 分区列 / 关键字段语义
  任一未知时，**不要** 输出可直接执行的改写 SQL；
- 仅在以上语义全部由用户确认后，才给出可执行的改写片段，并保持原
  SQL 的业务语义；
- 否则给出 “候选改写方向”，**显式标注为**
  `候选改写方向（待用户确认 <具体缺失项> 后再定稿）`，并说明每个
  方向各自适用于哪种确认结果，禁止伪装成可上线 SQL；
- **`LEFT JOIN` 被 `WHERE` 退化为 `INNER JOIN`（J005）属于纯文本可判
  问题**：候选方向只需说明 “保留左表全部行 → 过滤条件移到 `ON` /
  改为右表子查询” 与 “业务本来就是 INNER 语义 → 直接改写为
  `INNER JOIN`” 两类，落地前**只需在第 8 节确认是否保留左表全部行**，
  **不要**把右表唯一键 / 关联键唯一性写进 J005 的待确认项。
  右表唯一键 / 关联键唯一性的候选方向仅适用于 JOIN 行数膨胀 /
  多对多 / 双事实表 JOIN 等 J001/J002/J003/J004/J006/J010 风险。

## 7. 上线前验数 SQL
按以下顺序输出（缺项要写明原因，例如 “本 SQL 无 INSERT OVERWRITE，跳过 V2”）：

### V1. 源分区行数
```sql
-- ...
```

### V2. 目标分区行数
```sql
-- ...
```

### V3. 目标表主键唯一
```sql
-- ...
```

### V4. 核心字段空值
```sql
-- ...
```

### V5. JOIN 前后行数对比
```sql
-- ...
```

### V6. 昨天 vs 今天指标波动
```sql
-- ...
```

### V7. 近 7 天分区数据量
```sql
-- ...
```

### V8. COUNT DISTINCT 校验
```sql
-- ...
```

### V9. 右表关联键唯一性
```sql
-- ...
```

## 8. 需要我补充的信息
**只列出本次评审里实际被卡住的最小信息集合**，不要把它当成一个固定
清单逐项罗列。每条需写明 “为什么这次需要”（对应到第 1–5 节的具体
结论），且仅就当前 SQL 实际涉及的表和指标提问，不索取与本次评审
无关的元信息。

下面是常见候选项，**按需引用**；零 profile 模式下若无相关结论则
不要列出：

- 表分区列（若分区命名不在常见约定内）；
- 表粒度（针对触发 JOIN 行数膨胀 / 重复计算的具体表）；
- 唯一键（**仅**针对触发 JOIN 行数膨胀 / 多对多 / 双事实表 JOIN
  等 J001/J002/J003/J004/J006/J010 的具体右表索取；**不要**因为
  `LEFT JOIN` 被 `WHERE` 退化（J005）就索取右表唯一键——
  J005 只需确认 “业务是否要保留左表全部行”）；
- 表大小级别 small / medium / large（仅在评估 MAPJOIN / DISTMAPJOIN
  时索取）；
- fact / dim 标识（仅在区分 JOIN 关系时索取）；
- 是否允许近似去重（仅在指标含长周期 COUNT DISTINCT 时索取）；
- 目标表主键（仅在出现 INSERT OVERWRITE 时索取）；
- 是否调度任务（仅在涉及调度变量 / 回刷时索取）；
- LogView 数据（总耗时、慢 stage、reducer/joiner 数、长尾情况，
  仅在用户希望得到具体并行度数字时索取）；
- 业务对该指标的精确定义（仅就第 5 节实际出现且口径模糊的指标
  索取）。

如果上述项目对当前 SQL 都不构成阻塞，本节可写
“本次评审无被卡住的关键信息，仍建议在上线前结合 LogView / 数据量
复核第 4 节的性能建议”。
```

---

## 章节硬性约束

- 章节编号 0-8 不能跳过；
- 即使没有风险也必须输出 “暂未发现 ...” 占位文本；
- 第 0 节的 4 个字段（风险等级、是否上线、阻塞点、性能瓶颈、确认项）
  顺序固定；
- 第 4 节的 7 个子小节顺序固定，分别是：
  分区裁剪、Join 优化、Group By/Count Distinct 优化、
  年活/月活/长周期去重优化、动态分区优化、Reducer/Joiner/并行度建议、
  中间表/滚动表建议；
- **第 6 节产出口径**：表粒度 / 唯一键 / 分区列 / 字段语义任一未知时
  禁止输出 “可执行的改写片段”，必须输出标注 “候选改写方向
  （待用户确认 X 后再定稿）” 的草案；
- **第 8 节产出口径**：只列本次评审实际被卡住的最小信息集合，禁止
  把章节当成 “固定清单” 罗列；都不卡住时写
  “本次评审无被卡住的关键信息” 占位；
- 第 7 节按 V1-V9 顺序输出；
- 报告结尾建议加一句免责声明：
  > 本报告基于静态分析与业务上下文，未连接 ODPS。最终风险结论需要
  > 结合数据量、LogView、业务口径、调度上下文复核。
